# Local development only.
OPENAI_API_KEY="your-openai-api-key"

# Optional: limit which pages show up (langchain, openai, both).
APP_IMPL="langchain"

# Optional: show a sidebar toggle for reviewers (1 = on, 0 = off).
ALLOW_IMPL_SWITCH=0
